# OneTab Rewrite 功能清单

> 与 [README.md](./README.md) 配套。这里列出当前版本（v0.1.0）**实际实现**的功能，
> 以及**明确不做**的部分——避免"看起来应该有其实没有"。

## 一、保存标签页

- [x] 保存当前窗口全部标签页
  - 工具栏「保存当前窗口」按钮
  - 快捷键 `Alt+Shift+1`（可在 `edge://extensions/shortcuts` 改）
  - 网页右键菜单 / 扩展图标右键菜单
- [x] 保存单个标签页（网页右键 →「把此标签页存进 OneTab Rewrite」），保存后**不关闭**当前页
- [x] 保存所有窗口的全部标签页（扩展图标右键菜单 / 工具栏「保存所有窗口」）
- [x] 每次保存自动建一个以时间戳命名的新分组（`2026/09/09 10:19:07`），与官方 OneTab 行为一致
- [x] 批内按 URL 去重（同一批里重复的只留一条）
- [x] 自动跳过内部页面（`chrome://` `edge://` `about:` `devtools:` `view-source:` 等）
- [x] 保存后自动关闭已保存且非固定的标签页；**固定（pinned）标签页不会被关闭**
- [ ] 保存指定方向（左侧/右侧/其他标签页）
- [ ] 保存时自动按域名归并分组
- [ ] 定时自动保存

## 二、列表浏览

- [x] 虚拟滚动：DOM 常驻约 40 行，10 万条与 1000 条占用相同
- [x] 点击行打开标签页（新标签、前台）
- [x] `Ctrl` / `⌘` + 点击、鼠标中键 → 后台打开（不切走当前页）
- [x] 悬停显示行尾 `×` 直接删除（伪元素实现，零 DOM 成本）
- [x] 悬停行显示完整 URL（原生 title 提示）
- [x] favicon 图标（浏览器本地 `_favicon` API，不走网络）
- [x] 启动一次性读入全部数据到内存，滚动过程不访问数据库
- [ ] 行内缩略图预览
- [ ] 多选（框选 / `Shift` 连选）与批量操作
- [ ] 动态行高（超长标题换行显示）

## 三、分组

- [x] 新建分组（工具栏「＋分组」，创建时可命名）
- [x] 重命名分组（右键菜单，自绘输入框）
- [x] 折叠 / 展开（点击组行、右键菜单；折叠状态持久化）
- [x] 删除分组（组内条目整体进回收站，可再恢复）
- [x] 组行显示条目数 `分组名 (N)`
- [x] 「未分组」虚拟组：没有归属组的条目置顶显示，同样可折叠
- [x] 新分组排在最前（`order` 递减）
- [ ] 拖拽排序（组内、组间）
- [ ] 嵌套分组（子分组）
- [ ] 分组加锁（防误删）

## 四、回收站

- [x] 删除单条进入回收站（不是真删，只改 `groupId` 并记 `deletedFrom`）
- [x] 恢复到原分组（原分组已不存在则回到「未分组」）
- [x] 永久删除单条
- [x] 清空回收站（带确认）
- [x] 回收站默认折叠，条目灰色显示
- [ ] 回收站自动清理（如 30 天后）

## 五、搜索

- [x] 标题 + URL 子串匹配，实时过滤
- [x] 结果顶部显示命中条数
- [x] 搜索时自动排除回收站条目
- [x] 10 万条内存过滤，毫秒级（`?stress=100000` 下可用）
- [ ] 多关键词（空格分隔 AND）
- [ ] 正则 / 排除语法
- [ ] 按域名、日期筛选

## 六、导入导出

- [x] 导入官方 OneTab 数据（增强 JSON，含组名）
- [x] 导入纯 tab 数组 JSON（无组信息时按 `groupId` 自动建占位组）
- [x] 导入纯文本 URL 列表（官方「导出 URL 列表」格式，自动进「导入 YYYY-MM-DD」分组）
- [x] 导入时按 URL 去重，报告「导入 N 条，跳过 K 条重复」
- [x] 导出为文本（每行一个 URL，组名以 `#` 注释标记）
- [x] 导出为 JSON 备份（含全部条目、分组、回收站，可用于完整恢复）
- [ ] 导入本项目的 JSON 备份（目前只支持导入官方/文本格式）
- [ ] 导入浏览器书签 HTML
- [ ] 导入 Toby / Session Buddy / N-Tab / NiceTab 格式

## 七、键盘与右键菜单

- [x] `/` 聚焦搜索框
- [x] `↑` `↓` 在行间移动高亮，自动滚动跟随
- [x] `Enter` 打开标签页（在组行上则是折叠/展开）
- [x] `Delete` 删除当前高亮条目（进回收站）
- [x] `Esc` 清空搜索 / 关闭菜单与对话框
- [x] 右键菜单（条目）：打开 / 后台打开 / 复制 URL / 删除
- [x] 右键菜单（回收站条目）：恢复到原分组 / 永久删除
- [x] 右键菜单（分组）：折叠或展开 / 重命名 / 删除分组
- [x] 右键菜单（回收站组）：清空回收站

## 八、性能与诊断（本项目的特色）

- [x] 内建压测模式：`src/list.html?stress=N` 生成 N 条假数据（仅内存，刷新即恢复真实数据）
- [x] 性能验收脚本 `tools/bench_rewrite.js`：DOM 节点数、HitTest 平均耗时、滚动帧间隔 p95/max、长帧计数
- [x] 官方 OneTab 卡顿诊断报告与可复现脚本 `docs/diagnostics/`
  - `findings.md` 完整证据链
  - `dom_bench.html`（真实浏览器里的 DOM 形态基准）、`collect.js`、`hit_test_bench.py`、`scan_onetab_store.py`
  - `scripts/` 下为 Chrome trace 分析脚本

## 九、明确不做（v0.1.0 范围外）

- 云同步 / 多设备（涉及账号与服务端，与"本地优先、零依赖"冲突）
- 分享列表、协作
- 拖拽排序（虚拟列表里实现成本高，优先级低于性能）
- 暗色主题（欢迎 PR）
- Firefox 适配（manifest 需微调，欢迎 PR）
- 浏览器原生标签组（Edge tabGroups）双向同步
- 标签页休眠 / 内存优化（那是另一类工具做的事，本项目只管"存下来的列表"）

## 十、已知取舍

- 行高固定 26 px，超长标题截断为省略号（hover 显示完整 URL）
- 组内排序为「新保存的在上」，暂不支持手工调序
- `?stress=` 压测数据只存在于内存，不写入 IndexedDB
- 导入去重只看 URL，不比对标题
